#include<iostream>
using namespace std;
int main()
{
    int marks=45;      // marks stores student score
    if(marks>=40){     // if checks the pass condition
        cout<<"Pass";
    }else{                 //else executes when if condition is false
        cout<<"Fail";    //cout prints output
    }
    return 0;   //return 0 ends the program
}